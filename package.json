{
  "name": "k6-performance-tests",
  "version": "1.0.0",
  "description": "k6 performance tests for movie-catalog-api",
  "scripts": {
    "build": "node build.js",
    "smoke": "npm run build && k6 run dist/smoke.js",
    "load": "npm run build && k6 run dist/load.js",
    "stress": "npm run build && k6 run dist/stress.js",
    "spike": "npm run build && k6 run dist/spike.js",
    "smoke:url": "npm run build && k6 run dist/smoke.js -e BASE_URL=$BASE_URL",
    "load:url": "npm run build && k6 run dist/load.js -e BASE_URL=$BASE_URL"
  },
  "devDependencies": {
    "@types/k6": "^2.2.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}